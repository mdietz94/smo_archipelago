# Git hooks must keep LF line endings even on Windows. With core.autocrlf=true
# (the default on most Windows clones) `git checkout` would otherwise rewrite
# them to CRLF, and the `#!/usr/bin/env sh` shebang chokes when bash reads
# CR-terminated script lines.
.githooks/*  text eol=lf
