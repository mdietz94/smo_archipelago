# Force LF for all text files in this repo, overriding core.autocrlf=true.
# Without this, Windows git checkouts rewrite every file to CRLF, producing
# spurious ^M in diffs and editor views.
*            text=auto eol=lf
.githooks/*  text eol=lf
